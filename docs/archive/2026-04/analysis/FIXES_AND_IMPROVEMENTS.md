# Fixes and Improvements

## Сделано
- Подключил фронтенд к реальному API задач и пользователей: `task-manager-frontend/src/store/useAppStore.ts` (fetch/create/update/move, загрузка пользователей с лоудерами).
- Обновил дашборд под /api/dashboard и реальные задачи: `src/pages/DashboardPage.tsx` (KPI, графики, активные сотрудники, работник месяца, последние задачи, бюджет).
- Переделал страницу задач: `src/pages/TasksPage.tsx` (фильтры, поля приоритета/статуса/дедлайна/исполнителей, drag&drop загрузка файлов с отправкой на API, лоудеры).
- Канбан теперь использует серверные статусы и обновляет их через API: `src/pages/KanbanPage.tsx`.
- Финансы подключены к API транзакций/счетов с обработкой 403: `src/pages/FinancePage.tsx` (KPI, бар/пай графики, список транзакций).
- Аналитика подключена к /api/reports: `src/pages/ReportsPage.tsx` (KPI, графики эффективности, затраты по ролям).
- Команда грузится с /api/users: `src/pages/TeamPage.tsx` (лоудер, пустое состояние).
- Уведомления теперь отмечаются прочитанными при открытии: `src/components/Layout.tsx`.
- Визуальные правки под монохром: шрифт Inter, фон #f4f4f5, hover‑тени, анимация модалки fade+scale: `src/index.css`, `src/components/ui/Modal.tsx`.
- Карточка задач адаптирована под реальные данные (assignees, приоритет, статус, дедлайн): `src/components/ui/TaskCard.tsx`.

## Что осталось / рекомендовано
- Сверить пиксель‑перфект с Figma по всем экранам (отступы, радиусы 10–12px, тени, hover) и по компонентам KPI/Chart/Timer/Efficiency/AvatarGroup.
- Добавить просмотр/редактирование задачи (детали, комментарии, файлы) и кнопку "Добавить" в колонках Канбана, привязав к модалке/деталке.
- Доработать модалку: валидация обязательных полей, отображение ошибок API, превью загруженных файлов.
- Логин/регистрация перевести в светлую монохромную тему и убрать неиспользуемые dark‑классы.
- Расширить карточку команды (локация, навыки, часы) согласно ТЗ; при необходимости добавить поля в API.
- Ввести лоудеры/ошибки для всех API на страницах Dashboard/Tasks/Kanban/Team/Finance/Reports (часть уже есть, стоит унифицировать).
- Очистить репозиторий от `node_modules` и лишних ассетов (`hero.png`, `vite.svg`, `react.svg`).

## Запуск
```
cd task-manager-frontend
npm install
npm run dev -- --host
```
Убедитесь, что задан `VITE_API_URL` (или прокси `/api`) и бэкенд запущен (`task-manager-backend`, порт 5000 по умолчанию).
